# A file that belongs to no module, and therefore to no unit.
#
# A change to it affects EVERY unit: senro cannot tell what a Makefile above
# every module does, and running everything is the only answer that cannot
# skip a unit the change broke. See events/push-makefile.json.
all:
	@echo "nothing to do"
