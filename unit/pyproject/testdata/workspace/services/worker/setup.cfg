[metadata]
name = acme-worker
version = 0.1.0
description = A declarative setuptools distribution, configured without a pyproject.toml.

[options]
packages = find:
python_requires = >=3.12
install_requires =
    acme-core
