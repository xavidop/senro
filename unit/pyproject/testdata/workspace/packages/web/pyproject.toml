[project]
name = "acme-web"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["acme-store"]

[tool.uv.sources]
acme-store = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
