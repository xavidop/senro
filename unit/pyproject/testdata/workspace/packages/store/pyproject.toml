[project]
name = "acme-store"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["acme-core"]

[tool.uv.sources]
acme-core = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
